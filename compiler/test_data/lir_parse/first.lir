(define (my_function (first u64) (second u64)) u64
  (label (first)
    (set y (add u64 x y))
    (cond_jump y (first y) (third))
  )
  (label (second)
    (set y (add u64 x y))
    (jump (first y))
  )
  (label (third)
    (ret y))
)
